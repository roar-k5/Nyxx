build:
  builder: DOCKERFILE
  dockerfilePath: Dockerfile

deploy:
  startCommand: python -m uvicorn backend.server_v2:app --host 0.0.0.0 --port ${PORT:-5000} --workers ${UVICORN_WORKERS:-1}
  healthcheckPath: /health
  healthcheckTimeout: 30
  restartPolicyType: ON_FAILURE
  restartPolicyMaxRetries: 3
